import React from 'react';
import MainWrapper from '@components/MainWrapper';

const Home = () => {
  return (
    <div>
      <main>
        <MainWrapper />
      </main>
    </div>
  );
};

export default Home;
